[defaults]
inventory = inventory.yml
# The ssh_host_cert role comes from the oriolrius.pki_manager collection
# (see requirements.yml) — no local roles/ dir.
host_key_checking = True
stdout_callback = yaml
